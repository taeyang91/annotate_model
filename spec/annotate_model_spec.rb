# frozen_string_literal: true

RSpec.describe AnnotateModel do
  it "has a version number" do
    expect(AnnotateModel::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
